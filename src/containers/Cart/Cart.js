import React,{useContext, useEffect} from 'react';
import {cafesContext} from '../../contexts/CafesContext'

const Cart = () => {
    // const {getCart} = useContext(cafesContext);

    // useEffect(()=>{
    //     getCart()
    // }, [])

    return (
        <div className="cart">
            
        </div>
    );
};

export default Cart;